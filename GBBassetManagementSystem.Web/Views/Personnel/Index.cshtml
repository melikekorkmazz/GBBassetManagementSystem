@model IEnumerable<GBBassetManagementSystem.Entity.Entities.Personnel>
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<GBBassetManagementSystem.Web.SharedResource> Localizer

@{
    ViewData["Title"] = Localizer["Personnel"];
}

<div class="container-xxl flex-grow-1 container-p-y">

    @if (TempData["SuccessMessage"] is string successMessage)
    {
        <div class="alert alert-success alert-dismissible fade show"
             role="alert">

            <i class="icon-base ti tabler-circle-check me-2"></i>
            @successMessage

            <button type="button"
                    class="btn-close"
                    data-bs-dismiss="alert"
                    aria-label="Close">
            </button>
        </div>
    }

    <div class="card">

        <div class="card-header border-bottom">

            <div class="d-flex flex-wrap align-items-center justify-content-between gap-3">

                <div class="d-flex align-items-center gap-3">

                    <div class="avatar">
                        <span class="avatar-initial rounded bg-label-info">
                            <i class="icon-base ti tabler-users"></i>
                        </span>
                    </div>

                    <div>
                        <h4 class="mb-1">
                            @Localizer["Personnel"]
                        </h4>

                        <p class="text-muted mb-0">
                            @Localizer["PersonnelPageDescription"]
                        </p>
                    </div>

                </div>

                <a asp-controller="Personnel"
                   asp-action="Create"
                   class="btn btn-primary">

                    <i class="icon-base ti tabler-user-plus me-1"></i>
                    @Localizer["AddPersonnel"]
                </a>

            </div>

        </div>

        @if (!Model.Any())
        {
            <div class="card-body">

                <div class="text-center py-5">

                    <div class="avatar avatar-xl mx-auto mb-3">
                        <span class="avatar-initial rounded-circle bg-label-info">
                            <i class="icon-base ti tabler-users-off fs-2"></i>
                        </span>
                    </div>

                    <h5 class="mb-2">
                        @Localizer["NoPersonnelFound"]
                    </h5>

                    <p class="text-muted mb-0">
                        @Localizer["NoPersonnelFoundDescription"]
                    </p>

                </div>

            </div>
        }
        else
        {
            <div class="table-responsive">

                <table class="table table-hover">

                    <thead class="table-light">
                        <tr>
                            <th>@Localizer["RegistrationNumber"]</th>
                            <th>@Localizer["NationalIdentityNumber"]</th>
                            <th>@Localizer["FullName"]</th>
                            <th>@Localizer["Email"]</th>
                            <th>@Localizer["PhoneNumber"]</th>
                            <th>@Localizer["Department"]</th>
                            <th class="text-end">
                                @Localizer["Actions"]
                            </th>
                        </tr>
                    </thead>

                    <tbody>

                        @foreach (var person in Model)
                        {
                            <tr>

                                <td>@person.RegistrationNumber</td>

                                <td>@person.NationalIdentityNumber</td>

                                <td class="fw-medium">
                                    @person.FirstName @person.LastName
                                </td>

                                <td>@person.Email</td>

                                <td>@person.PhoneNumber</td>

                                <td>
                                    @(person.Department?.Name ?? "-")
                                </td>

                                <td class="text-end">

                                    <div class="d-inline-flex gap-1">

                                        <a asp-action="Details"
                                           asp-route-id="@person.Id"
                                           class="btn btn-sm btn-label-info">

                                            <i class="icon-base ti tabler-eye me-1"></i>
                                            @Localizer["Details"]
                                        </a>

                                        <a asp-action="Edit"
                                           asp-route-id="@person.Id"
                                           class="btn btn-sm btn-label-warning">

                                            <i class="icon-base ti tabler-edit me-1"></i>
                                            @Localizer["Edit"]
                                        </a>

                                        <a asp-action="Delete"
                                           asp-route-id="@person.Id"
                                           class="btn btn-sm btn-label-danger">

                                            <i class="icon-base ti tabler-trash me-1"></i>
                                            @Localizer["Delete"]
                                        </a>

                                    </div>

                                </td>

                            </tr>
                        }

                    </tbody>

                </table>

            </div>
        }

    </div>

</div>